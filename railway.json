{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "npm install && npm --workspace detector run build && npm --workspace web run build"
  },
  "deploy": {
    "startCommand": "cd web && npm start",
    "restartPolicyType": "ON_FAILURE"
  }
}
